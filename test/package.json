{
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "npm uninstall electron electron-prebuilt-path",
    "test": "node getPath.js && node index.js && electron index.js"
  },
  "dependencies": {
    "electron": "^1.4.10",
    "electron-prebuilt-path": ".."
  }
}
